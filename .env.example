# PostHog — same project as the React app and the marketing site so the
# marketing -> waitlist -> sign-up -> help -> app funnel lives in one stream.
# Leave empty for local dev to skip analytics entirely.
VITE_POSTHOG_PROJECT_TOKEN=
VITE_POSTHOG_HOST=https://eu.i.posthog.com
